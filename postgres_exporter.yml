# Exemple de postgres_exporter.yml
auth_modules:
  # Optionnel : utilisé si vous gérez plusieurs cibles via un module d'authentification
  chinook:
    type: userpass
    userpass:
      username: "chinook"
      password: "password_772"

